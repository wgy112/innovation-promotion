<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.sdu.mapper.SduProductDataMapper">

    <select id="findIdByName" parameterType="string" resultType="int">
        SELECT ID FROM sdu_product_data WHERE NAME = #{name}
    </select>

    <select id="findNext" parameterType="int" resultType="org.jeecg.modules.sdu.entity.SduProductData">
        SELECT * FROM sdu_product_data WHERE PID = #{pid}
    </select>

    <select id="findByKeyword"  parameterType="string" resultType="org.jeecg.modules.sdu.entity.SduProductData">
         SELECT * FROM sdu_product_data WHERE KEYWORD = #{keyword}
    </select>

</mapper>